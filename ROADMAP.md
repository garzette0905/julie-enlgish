# 앞으로 개발할 것

쥴리 잉글리쉬 홈페이지(<https://www.julieenglish.co.kr>)에 이어서 붙일 기능들입니다.
각 항목은 **지금 상태 → 해야 할 일 → 미리 정해야 하는 것** 순서로 적었습니다.

| # | 항목 | 성격 | 사전 준비 |
| --- | --- | --- | --- |
| 1 | 네이버 · 다음 검색 등록 | 코드 + 사이트 등록 | 없음 (바로 착수 가능) |
| 2 | 학부모 알림 문자 (SMS) | 코드 + 외부 계약 | 발신번호 사전등록, 문자 사업자 선택 |
| 3 | 원비 납부 링크 (PG) | 코드 + 외부 계약 | PG사 가맹 계약, 정산 계좌 |

---

## 1. 네이버 · 다음 검색 등록

### 지금 상태

- 도메인과 HTTPS는 이미 정상 동작한다.
- `<title>`, `description`, `og:*` 태그가 들어 있다.
- **`sitemap.xml` 이 없다.**
- **`robots.txt` 를 우리가 두지 않아서**, Cloudflare 가 만들어 주는 기본 파일이 대신 응답한다.
  (AI 크롤러 관련 문구만 들어 있고, 검색엔진에 필요한 sitemap 위치가 빠져 있다)
- `og:image` 가 `/assets/logo-512.png` 상대 경로다. 검색·SNS 미리보기에는 전체 주소가 필요하다.

### 해야 할 일

**(가) 코드 쪽 준비 — 등록 전에 먼저**

- [ ] `public/robots.txt` 추가 — 크롤링 허용 + `Sitemap:` 줄 명시
- [ ] `public/sitemap.xml` 추가
- [ ] `og:image` 를 `https://www.julieenglish.co.kr/assets/logo-512.png` 절대 주소로 변경
- [ ] 구조화 데이터(JSON-LD) 추가 — 학원은 `LocalBusiness` / `EducationalOrganization` 유형.
      상호, 주소, 전화번호, 영업시간을 넣으면 지역 검색에서 훨씬 유리하다
- [ ] 각 검색엔진이 주는 **소유 확인용 메타태그**를 `index.html` 에 삽입

**(나) 사이트 등록 — 원장님 계정으로**

- [ ] [네이버 서치어드바이저](https://searchadvisor.naver.com) — 사이트 등록 → 소유 확인 → 사이트맵 제출
- [ ] [다음 검색등록](https://register.search.daum.net) — 신규 등록 신청
- [ ] 네이버 스마트플레이스 / 카카오맵에 **업체 등록** (학원은 지역 검색 유입이 크다)
- [ ] Google Search Console 도 함께 (등록 방식이 같아서 품이 거의 안 든다)

### 미리 알아두실 것

이 홈페이지는 주소 뒤에 `#/reviews` 처럼 **`#` 을 붙여 화면을 바꾸는 방식**입니다.
검색엔진은 `#` 뒤쪽을 별개 페이지로 보지 않기 때문에, 지금 구조에서는 사실상
**홈 화면 하나만 검색에 잡힙니다.**

"쥴리 잉글리쉬" 같은 상호 검색에는 문제가 없지만,
"동백 영어학원", "초당 파닉스" 같은 검색으로 들어오게 하려면 화면마다 주소를 따로
가지도록 바꿔야 합니다(`#/reviews` → `/reviews`). 작업량이 적지 않으니,
**먼저 위 (가)(나)만 하고 유입을 지켜본 뒤 판단**하는 편이 좋겠습니다.

---

## 2. 학부모 알림 문자 (SMS 연계)

### 지금 상태

화면은 이미 만들어져 있고 **발송 버튼만 비활성화**되어 있다.

| 화면 | 위치 | 버튼 |
| --- | --- | --- |
| 관리자 → 원생 알림 | `public/js/admin.js` 의 `adminNotify()` | `#nt-send` |
| 관리자 → 원비관리 | `public/js/admin.js` 의 `adminTuitionScreen()` | `#tu-send` |

대상 선택과 내용 작성까지는 동작하므로, 실제 발송만 이어 붙이면 된다.
연락처는 이미 `users.phone`, `inquiries.parent_phone` 에 들어 있다.

### 해야 할 일

- [ ] 문자 사업자 선택 — 후보: 네이버 클라우드 SENS, 솔라피(구 쿨SMS), 알리고
- [ ] **발신번호 사전등록** (통신사 필수 절차, 서류 심사에 며칠 걸린다)
- [ ] Worker 에 발송 API 연동. 키는 시크릿으로만 넣는다 (레포에 올리지 않는다)

```bash
npx wrangler secret put SMS_API_KEY --name julie-enlgish
```

- [ ] `messages` 표 추가 — 보낸 내용, 대상, 성공/실패, 시각을 남긴다.
      실패한 건을 다시 보낼 수 있어야 하고, 요금이 나가는 일이라 기록이 꼭 필요하다
- [ ] 수신 동의 여부를 `users` 에 컬럼으로 추가 (아래 참고)
- [ ] 발송 전 확인창 — "○○명에게 보냅니다" + 예상 건수

### 미리 정해야 하는 것

**문자 종류.** 휴강 안내·원비 미납 안내는 **정보성 문자**라 수신 동의 없이 보낼 수 있지만,
"신규반 모집" 같은 **광고성 문자**는 법이 다릅니다.

- 광고성 문자는 제목에 `(광고)` 표기, 무료 수신거부 방법 안내가 의무입니다
- 광고성 문자는 **밤 9시 ~ 아침 8시 발송이 금지**됩니다
- 사전 수신 동의를 받아 기록으로 남겨야 합니다

두 종류를 섞어 쓸 계획이면, 회원가입·상담신청 화면에 **광고 수신 동의 체크박스**를
먼저 넣고 그 값을 저장해 두는 편이 안전합니다.

**카카오 알림톡도 검토해 볼 만합니다.** 건당 요금이 문자보다 싸고 학부모가 읽을 확률도
높습니다. 다만 템플릿을 미리 등록해 승인받아야 하고, 카카오 채널이 필요합니다.
(실패하면 문자로 자동 전환하는 방식이 일반적입니다)

---

## 3. 원비 납부 링크 (PG사 연계)

### 지금 상태

- `tuition` 표에 **월별 금액 · 납부 여부 · 특기사항**이 이미 기록된다
  (`migrations/0005_tuition.sql`)
- 관리자 화면에서 손으로 납부 체크를 하고 있다
- 결제 자체에 필요한 정보(주문번호, 결제수단, PG 거래번호)는 아직 없다

### 해야 할 일

- [ ] PG사 선택 — 후보: 토스페이먼츠, 나이스페이먼츠, KG이니시스, 카카오페이
- [ ] **가맹점 계약** (교습소 사업자등록증, 정산 계좌 필요. 심사에 1~2주)
- [ ] `tuition` 에 결제 칼럼 추가 — `order_id`, `pg_tid`, `pay_method`, `pay_status`, `paid_at`
- [ ] 학부모용 납부 화면. 로그인 없이 열 수 있는 **1회용 링크**가 편하다
      (예: `/pay/<토큰>` — 토큰에 학생·해당월을 묶고 유효기간을 둔다)
- [ ] PG 웹훅 수신 → 입금이 확인되면 `tuition.paid` 를 **자동으로** 켠다
- [ ] 납부 완료 시 원장님에게 알림 (지금 쓰는 텔레그램 알림에 얹으면 된다)
- [ ] 영수증 / 현금영수증 처리 방식 정하기

### 미리 알아두실 것

**카드 번호는 우리 서버에 절대 저장하지 않습니다.** 결제창은 PG사가 띄우고,
우리는 "이 주문이 결제됐다"는 결과만 받습니다. 이 구조를 유지해야
개인정보·보안 부담이 생기지 않습니다.

**웹훅 검증이 핵심입니다.** PG가 보내주는 결제 완료 신호는 반드시 서명을 확인하고,
**금액이 우리가 청구한 금액과 같은지 대조**한 뒤에 납부 처리해야 합니다.
이 검증이 빠지면 결제 없이 납부 완료로 만들 수 있습니다.

**중복 결제 방지.** 같은 학생의 같은 달에 두 번 결제되지 않도록
`(user_id, month)` 에 이미 걸려 있는 유일성 제약을 결제 단계에서도 확인해야 합니다.

---

## 그 밖에 생각해 둔 것

- [ ] 원비 미납 자동 알림 — 매월 정해진 날 미납자에게 자동 발송 (2번 완료 후, Cron Trigger)
- [ ] 학부모가 자녀의 납부 내역을 직접 확인하는 화면 (3번 완료 후)
- [ ] 후기 게시판 스팸이 늘어나면 승인제 전환 또는 간단한 확인 절차 추가
- [ ] `Always Use HTTPS` 켜기 — Cloudflare → julieenglish.co.kr → SSL/TLS → Edge Certificates
      (지금은 `http://` 로 들어와도 그대로 열려서, 상담신청 폼이 암호화 없이 전송될 수 있다)
